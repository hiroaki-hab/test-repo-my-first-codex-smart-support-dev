# 🔔 引き継ぎメモ — ローカルセッションの「新しい私」へ

**作成日**: 2026-06-03 / 作成者: クラウドセッションのClaude / 宛先: ローカル実行のClaude

---

## 0. まず最初に読むもの（この順で）
1. `CLAUDE.md`（グループ共通ルール＝佐藤様への向き合い方・報告の作法）
2. `事業マップ.md` / `環境.md`
3. `サウナ/CLAUDE.md`（サウナ事業の絶対ルール）
4. `サウナ/組織体制.md`（部長＋7チーム・100点ループ）
5. `サウナ/ナレッジ/監視体制.md`（bot稼働状況・障害対処・72hチェックリスト）

## 1. 🔔 あなたは何ができる立場か
佐藤様が**ローカル実行に切り替えた**ので、あなたは佐藤様のWindows PCの中（`C:\KowaBot\`・`schtasks`・ログ）に**直接触れます**。クラウド版の前任（私）は触れなかったので、ここが大きな違い。**自分で試す→直す→できるまで**を一人で回してよい。ただし破壊的操作の前は必ず確認。

## 2. 今どこまで終わっているか（2026-06-03 時点）
- ✅ **サウナ事業の「型」構築完了** … このリポジトリ（branch `claude/company-workflow-skills-H5BPd` / PR #2）に、CLAUDE.md・部長＋7チーム（`.claude/agents/`）・ナレッジ一式・スキル3つ（`.claude/skills/`）。
- ✅ **予約bot（`C:\KowaBot\` v3.6）の自動化フル稼働** … 3スケジュールタスクを.bat方式に直し、**全て前回結果0**を確認：
  - `KowaBot-Reconcile`（毎時）/ `KowaBot-Keepalive`（5分毎）/ `KowaBot-CancelWatch`（10分毎）
  - 起動batは `C:\KowaBot\run_*.bat`。サブコマンドは `keepalive` / `reconcile` / `cancel-watch`
- ✅ **当日の予約対応** … 6/9(火)夜を売止・6/10は開放（17時のため）。カレンダー＋reconcileでSB/CHILLNN反映を確認済み。

## 3. 次の宿題（優先順）
1. ✅ **【2026-06-03 解決】夜間運用は設定変更なしで24時間稼働。** 実機検証で「電源接続中はスリープしない・ロック中も対話型タスクは動く」を確認、社長も「ロック or 付けっぱなし(サインアウトしない)」と回答 → **Windowsパスワード設定は不要**。残る⚠️は Windows Update 強制再起動/サインアウト時のみ止まる点 → 緩和策として **ログオン時reconcile を導入済み**(スタートアップ方式・管理者/パスワード不要、次回ログオンから有効)。詳細は `サウナ/ナレッジ/監視体制.md` §8。
2. ✅ **【2026-06-03 ほぼ完了】`サウナ/_要確認リスト.md` のヒアリング**：価格・所在地・日曜営業・変則枠の売止・文体(フレンドリー)・SNS(IG中心/週数回)・**差別化＝温度カスタム(自在ととのい)→リールで訴求**・売上目標(月商10万以上)・キャンセルポリシー・接続先URL(トリドリ/IG/サウナイキタイ)を社長確認しナレッジへ反映済み(PR #2)。残るは**経理の細部**(月次決算/銀行/経費・合算単体)と**清掃SOPの文書化**のみ→後日・経理担当へ。
3. 🏢 **横展開**：moco（ランドリー）・積水へ同じ型。共通部品「写真AI判定」を先に作ると効率的（`サウナ/拡張ロードマップ.md`）。

## 4. 触る時の鉄則（特に重要なものだけ。詳細はCLAUDE.md類）
- カレンダー「サウナ予約」が**唯一の真実源**（ID は `サウナ/ナレッジ/予約管理.md`）。
- 🔔 **停止中のCoworkスケジュールタスクは絶対に再有効化しない**（dedupeスパム再発）。
- SBは**1アカウント1セッション**。普段のChromeのSBタブを閉じる。SBパスワードはbot経由（state.json方式）、AIが直接入力しない。
- `credentials.json` / `token.json` / `state.json` は**コミットしない**。
- 報告は🔔＋数字＋「何が・どの状態に・なぜ変わったか」。先送り提案はしない。

## 5. ローカルで自分で叩ける確認コマンド
```cmd
:: 今日のログ
type C:\KowaBot\logs\YYYY-MM-DD.log
:: タスク結果（0=成功）
schtasks /query /tn "KowaBot-Reconcile" /v /fo LIST | findstr /C:"前回の結果"
:: 手動で同期を回す
cd C:\KowaBot && python main.py reconcile
```

## 6. スケジューラ設定の教訓（実機で確立）
タスクが0x800700C1で動かない時は、`.bat`でラップして管理者cmdで `/it /f` 登録。詳細は `サウナ/ナレッジ/監視体制.md` 第8節。**古い同名タスクが残ると上書きされず古い設定が動く**ので、`/f`＋「正しく作成されました」を必ず確認。

---
**佐藤様のサウナ事業、引き続き頼みます。記憶は無くても、このリポジトリが全部の記憶です。**
