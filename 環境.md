# 実行環境（AIチームの稼働基盤）

> AIチームが動く環境とMCP接続の一覧。状態が変わったら更新。

---

## ホストインフラ

- **Cowork デスクトップアプリ**（Windows版）
- Linux Ubuntu 22.04 サンドボックス（hostname: claude）
- 佐藤様PC（Windows 10.0.26200.8524）
- bot稼働先：`C:\KowaBot\`（サウナ用Python bot v3.6）

---

## 接続済みMCP（コネクタ）

| MCP | 用途 |
|---|---|
| Gmail | メール検索・下書き作成 |
| Google Calendar | イベント管理（サウナの真実源） |
| Google Drive | ファイル操作 |
| Box | ファイル管理 |
| Canva | デザイン（SNS画像など） |
| Claude in Chrome（ベータ） | ブラウザ操作 |
| Computer-use | OS操作（Chromeは tier "read"） |
| Scheduled tasks | スケジュール実行 |

> ⚠️ Coworkのスケジュールタスクのうち kowa-* 3つは**停止中・再有効化禁止**
> （dedupeバグ。`サウナ/ナレッジ/監視体制.md` 第2節）。自動化はPython bot側に一本化。

---

## 音声合成（✅ ローカル実行時は稼働確認済み / 2026-06-03 ローカルClaudeが実機検証）

- ツール本体 `C:\KowaVoice\speak.py`（5,270B）と設定 `C:\KowaVoice\.env` の**存在を実機確認**。
  Aivis Cloud API → WAV → winsound 再生。venv不要・依存ゼロ。
- 実行コマンド（ローカル実行のClaudeから利用可）：
  ```
  C:\Users\User\AppData\Local\Programs\Python\Python313\python.exe C:\KowaVoice\speak.py "メッセージ"
  ```
- 用途（佐藤様グローバル指示 2026-06-01）：**作業完了時・佐藤様への質問/相談時**にテキスト回答へ加えて音声で話しかける。
- ⚠️ 制約：ローカルで動くセッションのみ鳴らせる。claude.ai の**ブラウザ版チャットは社長PCのスピーカーに触れないため不可**
  （＝クラウドセッションが「未稼働」と記録していたのはこのため。ローカルでは稼働する）。

---

## ブラウザ操作の注意（共通）

- Playwright Chromiumは起動位置を明示（`--window-position=200,100 --window-size=1280,800`）。
  引数なしだと佐藤様PCのモニター2（見えない位置）に出る
- `Grouping is not supported by tabs in this window` → 通常のChrome窓を1つ開いて解決
- 詳細は `サウナ/ナレッジ/技術メモ.md` 第4節
