# 監視体制（ライブ・オペレーション）

> 「いま何が自動で動いているか」「毎日何を確認するか」の運用ステータス。
> 2026-06-02 18:14 時点の状態を基準に記録。状態が変わったら更新する。

---

## 1. 稼働中の自動化（Python bot v3.6）

佐藤様PC `C:\KowaBot\` で動作中。Windowsタスクスケジューラに3つ登録：

| タスク | 間隔 | 役割 | 状態 |
|---|---|---|---|
| **KowaBot-Keepalive** | 5分毎（営業時間中） | SB＋CHILLNN のセッション維持 | ✅稼働中 |
| **KowaBot-Reconcile** | 1時間毎 | カレンダー基準でCHILLNN＋SBを全件突合・修正 | ✅稼働中 |
| **KowaBot-CancelWatch** | 10分毎 | Gmailのキャンセル通知監視（実装は最小限） | ✅稼働中 |

### PCを起動したまま放置したときの自動挙動
1. 5分毎：bot が SB に navigate して Cookie をアクティブ維持
2. 1時間毎：Googleカレンダーを読み、CHILLNN/SB の差分を修正
3. 10分毎：Gmailのキャンセル通知をチェック（処理は手動 or 後日実装）

---

## 2. 🔔 停止中のシステム（絶対に再有効化しない）

**Coworkスケジュールタスクは全停止中。** 件名dedupeバグでGmail下書きが60通超に増殖したため。

| タスクID | 元の役割 | 停止理由 |
|---|---|---|
| kowa-keepalive | 10分毎 SB+CHILLNN維持 | 件名不一致でdedupe失敗、下書き60通超 |
| kowa-smart-sync | 15分毎トリガー検知＋クロスロック | 同上 |
| kowa-sauna-daily-sync | 1日3回フル整合性監査 | 同上 |
| daily-inbox-summary | 平日朝7時Gmail要約 | 不使用 |
| moco-line-draft-prep | moco用LINE下書き | 不使用 |

> ⚠️ dedupeバグが未修正のため、**この3つ（特に上3つ）を再有効化するとスパムが再発**する。
> 自動化はPython bot側（C:\KowaBot\）に一本化する方針。

---

## 3. 毎日の確認方法

### Python bot のログ
```cmd
type C:\KowaBot\logs\YYYY-MM-DD.log
```
読みどころ：
- `SB session alive: True/False` … SBセッションの生死
- `CHILLNN session alive: True/False` … CHILLNNセッションの生死
- `=== End: keepalive OK ===` … keepalive正常
- `=== End: reconcile OK ===` … 同期完了

### 🔔 緊急対応の合図
- `SB session alive: False` が**連続**したら緊急対応（手動再ログインが必要な可能性）
- ⚠️ 現状、SB切れの**即時通知は未実装**（botは黙ってログに残すだけ）。v3.7の最優先課題

---

## 4. 毎日の引継ぎチェックリスト

- [ ] `C:\KowaBot\logs\` を確認、`SB session alive: False` 連続なら緊急対応
- [ ] Googleカレンダー「サウナ予約」を真実源として扱う
- [ ] 佐藤様の個人カレンダー（primary）も読む（ゴルフ・飲み会・誕生日を営業に連動）
- [ ] **Coworkスケジュールタスク3つを再有効化しない**（dedupeバグ未修正）
- [ ] 普段使いのChromeでSBタブを開きっぱなしにしない（同時1セッション制限）
- [ ] credentials.json / state.json はバックアップ価値あり（紛失＝再認証）
- [ ] 月初にSB「毎月の受付設定」を実施（忘れるとシフト編集不可）

---

## 5. 既知の不具合と v3.7 ロードマップ

| 問題 | 影響度 | 予定 |
|---|---|---|
| **SBセッション切れの即時通知が未実装** | 高 | v3.7 最優先 |
| SB空セルへの新規追加 `no_block0`（5件残） | 中 | v3.7で対応 |
| 7/20 海の日など祝日判定の取りこぼし | 低（一部対応済） | v3.7で完全対応 |

> これらは**開発タスク**（bot側の改修）。佐藤様への確認待ち項目は `../_要確認リスト.md` を参照。

---

## 6. 後任AI 最初の72時間チェックリスト

引き継ぎ直後はこの順で立ち上げる。

### Day 1（引継ぎ日）
- [ ] `C:\KowaBot\logs\` の今日のログを開き、エラーがないか確認
- [ ] Coworkスケジュールタスク一覧で kowa-* 3つが disabled か確認
- [ ] Googleカレンダー「サウナ予約」の今週分を目視確認
- [ ] CHILLNN adminの佐藤様ログイン状態を確認
- [ ] HPB予約があれば 100pt確認＋カレンダーに警告追記

### Day 2
- [ ] daily reconcile が実行されたかログ確認
- [ ] SB 6月＋7月のシフトを目視確認、変な穴がないか
- [ ] Gmail下書きに新規エラー通知が出ていないか確認

### Day 3
- [ ] 1週間分のキャンセル・新規予約を確認
- [ ] HPB予約のポイント警告漏れがないかカレンダーで点検
- [ ] 佐藤様に「困っていること」を直接ヒアリング

### Day 4以降
- [ ] v3.7課題（SB空セル / HPBポイント警告 / cancel-watch）に着手
- [ ] 清掃管理・積水ハウス連携など事業拡張の検討開始（`../拡張ロードマップ.md`）

---

## 7. 履歴の参照先

構築の経緯（2026-06-01〜02の成功ログ・修復作業・時系列）は `../引き継ぎ書_原本.md` の追補に保存。

---

## 8. タスクスケジューラの正しい設定（実機で確立 / 2026-06-03）

### 症状（毎朝勝手に動かなかった原因）
- `前回の結果: -2147024703`（= `0x800700C1` = プログラムを起動できない）
- タスクの実行コマンドが `py C:\KowaBot\main.py` で、**`reconcile` 等のサブコマンドが抜けていた**
- かつ `ログオン モード: 対話型のみ` ＝ ログオフ中は走らない

### 🔔 正しい設定：.bat でラップする方式（これが安定）
1. 起動用batを作る（例：reconcile）
   ```
   echo @echo off> C:\KowaBot\run_reconcile.bat
   echo cd /d C:\KowaBot>> C:\KowaBot\run_reconcile.bat
   echo python main.py reconcile>> C:\KowaBot\run_reconcile.bat
   ```
2. **管理者cmd**でタスクを作り直す（`/it`＝パスワード不要・対話実行、`/f`＝上書き）
   ```
   schtasks /create /tn "KowaBot-Reconcile" /tr "C:\KowaBot\run_reconcile.bat" /sc hourly /st 19:55 /it /f
   ```

### トラブル対処（実機でハマった点）
- 「**実行者パスワードを入力**」「**アクセスが拒否されました**」
  → cmdが管理者でない。**管理者として実行**したcmdで `/create /f /it` する（`/it`でパスワード不要）
- 確認方法：
  ```
  schtasks /query /tn "KowaBot-Reconcile" /v /fo LIST | findstr /C:"前回の結果"
  findstr /C:"reconcile OK" C:\KowaBot\logs\YYYY-MM-DD.log
  ```
  `前回の結果: 0` ＋ ログに `=== End: reconcile OK ===` が出れば成功。

### 状態（2026-06-03 全タスク修正完了）
- ✅ サブコマンドは `keepalive` / `reconcile` / `cancel-watch`（`python main.py --help` で確認済み）
- ✅ **3タスクとも .bat 方式に作り直し、`前回の結果: 0` を確認**：
  - `run_reconcile.bat` → `schtasks /create /tn "KowaBot-Reconcile" /tr "C:\KowaBot\run_reconcile.bat" /sc hourly /st 19:55 /it /f`
  - `run_keepalive.bat` → `schtasks /create /tn "KowaBot-Keepalive" /tr "C:\KowaBot\run_keepalive.bat" /sc minute /mo 5 /it /f`
  - `run_cancelwatch.bat` → `schtasks /create /tn "KowaBot-CancelWatch" /tr "C:\KowaBot\run_cancelwatch.bat" /sc minute /mo 10 /it /f`
- ⚠️ **同名の古い壊れたタスクが残っていると、作り直したつもりでも古い設定が動く**
  → 必ず `/f` で上書きし、「**正しく作成されました**」が出ることを確認（`/run` の「実行が試行されました」は別物）
### 🔔 夜間運用は設定変更なしで24時間稼働(2026-06-03 ローカルClaudeが実機検証＋社長確認)
実機で裏取りした事実：
- ✅ **電源接続中はスリープ/休止しない**（`powercfg` AC設定 STANDBYIDLE/HIBERNATEIDLE とも `0x0`＝なし）
- ✅ **画面ロック中も「対話型のみ」タスクは動き続ける**（ロック≠サインアウト）
- ✅ 社長の運用は「**ロック or 付けっぱなし**（サインアウトしない）」と確認（2026-06-03）
- ⇒ **Windowsパスワード設定は不要**。現構成のまま24時間動く。

残る⚠️（パスワードなしでは完全には防げない）：
- **Windows Update の強制再起動・うっかりサインアウト/シャットダウン**が起きると、
  その間〜次回ログインまで止まる（「ログオンの有無に関わらず実行」に変えるには結局パスワードが要る）。
- アカウントは現在 `Password required: No`（空パスワード）。空パスワードは batch logon に使えないため、
  もし将来「ログオフ中も動かす」を選ぶなら**まずWindowsパスワード設定が前提**になる。
- 緩和策 ✅**導入済み(2026-06-03)**：**ログオン時に reconcile を1回自動実行**。
  再起動/サインアウトで空いた穴を、次回ログイン直後に自動で埋める保険。
  - Task Scheduler の `/sc onlogon` は**管理者権限を要求して拒否**されたため、
    **管理者不要のスタートアップフォルダ方式**を採用（機能は同等）。
  - 実体: `C:\KowaBot\run_reconcile_onlogon.bat`（ネット安定待ち30秒→`python main.py reconcile`）
    を、スタートアップの `KowaBot-ReconcileOnLogon.vbs`（WScript で**ウィンドウ非表示起動**）が呼ぶ。
  - スタートアップ場所: `…\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\`
  - ⚠️ `.bat`/`.vbs` はローカルPCのみ。リポジトリにはコミットしない（C:\KowaBot 配下は bot 本体扱い）。

### 🔔 cmd窓を出さない（2026-06-03 社長指示「作業中に窓が突然出て邪魔」）
- 原因：3タスクが `/it`（対話実行）で `run_*.bat` を直接起動 → reconcile時は約30秒コンソールが居座る
  （keepalive 5分毎・cancel-watch 10分毎も同様に窓を出していた）。
- 試した没案：`schtasks /change` で実行先を wscript に変更 → **「run as password」を要求**され停止
  （`/it`タスクの変更は資格情報の再入力が必要・空パスワードでは不可）。「仮想デスクトップ2に固定」も
  一瞬出る窓を狙って送るのは不安定で没。
- ✅ 採用：**各 `run_*.bat` を「`start "" wscript //nologo C:\KowaBot\hide_*.vbs`＋即終了」に書き換え**。
  wscript はコンソール無し → 窓が出ない。`hide_*.vbs` が `Run(cmd,0,True)` で python を非表示実行。
  - 生成物（ローカルのみ・非コミット）：`hide_reconcile.vbs` / `hide_keepalive.vbs` / `hide_cancelwatch.vbs`
  - 管理者・パスワード不要。タスクの実行先（run_*.bat）は変更不要なので資格情報プロンプトも出ない。
- ⚠️ **副作用：タスクの「前回の結果」は常に 0（撃ちっぱなし）になる**。bat が wscript を起動して即終了するため。
  → **成否は必ずログで確認**（`=== End: reconcile OK ===` 等）。「前回の結果: 0」は成功の証拠にならなくなった。
- ⚠️ **残る一瞬のチラつき**：タスクが bat を起動する瞬間だけコンソールが数十msだけ出る。完全ゼロにするには
  タスクの実行先を wscript 直呼びに変える必要があり、それには**管理者＋Windowsパスワード**が要る（未対応）。
- 🔔 **教訓（最重要）：`.bat` のコメントは必ず ASCII（英語）で書く**。cmd は .bat を cp932 で読むため、
  UTF-8 の日本語 `rem` コメントが文字化けして `'...' は認識されません` エラーになり、一瞬の窓にエラーが出る。
  日本語の説明は .vbs（wscript が許容）かこのドキュメント側に書く。

### 運用前提
- 🔔 **PCは電源を入れたまま・サインアウトしない**（ロックはOK）。これだけで bot は24時間動く。

---

## 9. 🔔 2026-06-06 インシデントと恒久対策（部長対応）

社長指摘「予約がカレンダー未反映／6/14が売止／監視を見直して」。調査で3点判明・対処済。

### (1) reconcile（毎時の全件同期）が6/4夜から実体0回だった ← 最重要
- 症状：スケジューラは点火し「前回の結果: 0」だが、ログに `Start: reconcile` が**一切出ない**＝python実体が動いていない。keepalive/cancel-watchは正常稼働（6/6: keepalive 125回・cancelwatch 63回に対し reconcile 0回）。
- 真因：bat/vbsが呼ぶ `python` が **WindowsApps の Store エイリアス**（`…\WindowsApps\python.exe`）。wscript隠し起動かつ約45秒かかる長い reconcile だと**無音で失敗**していた（短いkeepaliveは同チェーンで成功するので気づきにくい）。
- 恒久対策：**`hide_reconcile.vbs` を本物の pythonw.exe フルパス直叩きに変更**（cmd・Storeエイリアス排除・窓無し）。本番経路で実行・完走を確認済（10:49）。
  - 実体: `"C:\Users\User\AppData\Local\Programs\Python\Python313\pythonw.exe" "C:\KowaBot\main.py" reconcile`
  - ✅ keepalive/cancelwatch の vbs も同日 本物pythonw直叩きに統一済（hide_keepalive.vbs / hide_cancelwatch.vbs）。3つとも稼働確認済。
  - ⚠️ 既存の3タスクは**管理者作成で保護**されており、非管理者では変更・削除・上書き(`/create /f`)が全部 Access denied（新規名の作成だけは可）。
  - 🪟 窓のチラつき＝タスクが `run_*.bat` を起動する瞬間のコンソール。完全に消すにはタスクを bat廃止→wscript直起動に作り替える必要があり**管理者必須**。
    そのためのワンクリック用 **`C:\KowaBot\fix_tasks_windowless.bat`（右クリック→管理者として実行）** を用意済。実行すると3タスクが `wscript //nologo hide_*.vbs` 直起動になり窓ゼロ。

### (2) SBセッションが毎日切れる（keepaliveでも防げない）
- 事実：切断時刻が **6/4=11:19 / 6/5=11:09 と2日連続「11時台」で一致**。6/3は終日生存。keepaliveは生きている＝**アイドル切れではない**。
- 最有力原因：**1アカウント1セッションの奪い合い**。人間（社長orスタッフ）が11時頃にHPB/サロンボードを自分のブラウザで開く→botセッションが弾かれる。keepalive（アイドル維持）では原理的に防げない。
- 対策候補：①**切断の即時通知**実装（v3.7最優先・config.pyに `SB_SESSION_LOST_SUBJECT` 定数だけ存在し未配線）②人間はHPBを別ブラウザ/スマホで見る運用統一 ③自動再ログイン（PW保持＝要社長判断）。
- 再ログインは人間が `py setup_login.py`（AIはPW入力しない）。

### (3) CHILLNN 6/14 休日プランが誤売止だった
- 6/14(日)は「休日プラン販売・平日プラン売止」が正。一時的に休日プランまで売止になり、botが6/1・6/4と繰返し修正していた。2026-06-06に本番reconcileで未反映20件を適用＋**再読込で差分なし＝定着を確認**。現在は正常。

---

## 10. 🔔 2026-06-16 インシデント PDCA（SBセッション切れ→6/18ダブルブッキング危機）

### 事象（時系列）
- **06:59** SBセッション切断。06:58まで正常にSBシフト設定 → 1分後に死亡＝「1アカウント1セッションの奪取」と一致。`session_notify`(6/15配線済)が下書き発報＝**即時通知は機能した**。
- **09:02** HPB経由で新規予約：**港谷 太一様 6/18(木)13:00–16:00 大人2名／平日3時間¥4500クーポン／ポイント2,600pt利用**。SB停止中で自動取込み不可。
- 同6/18 **14:00–17:00 に「キャンプフロンティア取材 4名(確定・5/27〜)」**。完全個室で13–16が重複＝**ダブルブッキング**。社長指摘「予約被ったぞ」。

### P（計画・仮説）
- 切断の根治は**botと人間でSBログインIDを分ける**ことだと仮説（→HPBサポートに確認）。
- 6/18が売れた原因の仮説＝**取材ぶんの売止がSB実機に届いていない**。

### D（実施）
- SB手動再ログインで復旧（`setup_login.py`）。**ワンクリック化**＝デスクトップ「SB再ログイン」bat新設。⚠️初版はUTF-8日本語でcmdが文字化けし起動失敗→**純ASCIIに作り直して解決**（§8の教訓「batは英語で」を再確認）。**09:36 `SB session alive: True` 復旧確認**。
- 港谷予約をカレンダー(真実源)へ手動反映＋⚠️重複明記。**社長判断＝取材優先→港谷様に別時間/別日を依頼**。
- 6/18昼に `[KÖWA/売り止め] 昼クローズ（取材のため）` を新設（次の重複予防）。
- 港谷様へお詫び＋変更依頼メッセージ作成（**300字以内・HPBは返信不可のため電話返信導線 022-358-6092 入り**。案【A】6/18 18:00〜=他スタッフ対応／【B】6/19・23・24の13:00〜）。

### C（評価・判明事実）
- **whitebox検証**：bot現コードは取材(14–17)を6/18 13–16の売止対象として正しく計算（memoに取材名が出る）＝**「botが取材を無視」ではない**。`reconcile.py`は6/2以降変更なし。
- **SB実画面(社長提供)**：6/18は13–16に売止が無く16:00以降のみ「予定あり」＝**売止がSB実機に届いていなかったのが確定**（=予約が通った理由）。朝のreconcileも6/18を“差分なし”扱い＝**botの認識と実機がズレていた疑い**。
- **横展開**：6/22(マツカワ/電話)・6/25(佐藤/CHILLNN)もSB売止が未反映（社長発見）＝**同種「売止がSBに届かない」が複数日**。
- **HPBサポート確認(春日さん)**：①同一ID2か所＝片方ログアウト／別IDなら同時OK ②**スタッフIDはシフト編集(売止)不可**／予約管理用ID(阿子島発行)or管理者IDならシフト編集可 ③同一PCでも**別ブラウザならOK**。

### A（恒久対策・次アクション）
1. **SBログインIDを bot と人間で分離（根治）**：bot=現管理者ID(CE52480)継続／人間=別ID。CS用に**スタッフID**（即発行可）、売止用に**予約管理用ID**（阿子島へ依頼）。**人間はbotのIDに二度とログインしない**。
2. **デスクトップ2に専用Chrome＋Claude-in-Chrome**を構築し、AIがSBを独立操作（社長案・採用）。接続確認済(Browser 1)。
3. **取材/貸切/私用で枠を潰す予定は必ず `[KÖWA/売り止め] 〜` 名で入れる**（プレフィックス無し手動予定は“効いたつもり”事故の元）。
4. **売止がSB実機に届いているかをreconcile実機で再検証**し、6/18・6/22・6/25のズレ原因（認識ズレ/無音失敗）を特定（港谷様を6/18から移動後に実施）。
5. 再ログインの**ワンクリックbatは純ASCIIで作る**（文字化け回避）。

### 残課題（フォロー）
- 港谷様の振替日確定 → SB予約日変更(ポイント引継ぎ) → カレンダー更新 → 6/18完全クローズ。
- 🔴 **6/22(月)13-16(電話マツカワ2名)がSBで売止できていない＝`no_block0`バグ**（空セルへの売止追加が黙って失敗）。09:55 reconcileが`SB set day22 (no_block0)`→「8件修正完了」と**成功ログを出したのに、10:47 dry-runで再び「- → 13:00-18:00」＝実機未反映**を確認（＝撃ちっぱなし・ログは当てにならない）。**手動売止が必須**。6/25(佐藤CHILLNN)は売止済み(差分なし)。
- 🔔 **追加で判明した根本原因＝`no_block0`バグはクロスロック売止の主因**：他チャネル予約で“空き枠を塞ぐ”操作（=被り防止の本丸）がSB実機に反映されない。**SBセッション切れ（書込み不可）と並ぶ「売止がSBに届かない」二大原因**。当面の運用＝reconcile後に必ずdry-runで未反映を検知し手動補完。bot本修正(sb_manager 空セル売止)はv3.7最優先級。6/18の取材売止が事前に効かなかった件も、ほぼこのバグが原因と推定。
- 予約管理用ID発行（阿子島）→ デスクトップ2セットアップ #2〜#6。

---

## 11. ✅ 2026-06-18 解決：自動再ログイン＋no_block0根治（§10残課題のクローズ）

§9(2)「毎日切れる」と §10残課題「no_block0で売止が黙って失敗」を**実機で根治・実証**した。

### 真因（実DOMで確定）
- **SBは夜間に自動失効**（idle timeout。6/18も「一定時間操作されなかったため…」のエラー画面を実機確認）。keepaliveでは防げない＝毎朝の再ログインが必要だった。
- **no_block0の正体＝「休日」セルには時間枠(`yoteiBlock0`)が存在しない**。`get_month_shifts` は休日を「-(開いてて売止なし)」と誤読→`diff_sb`が「売止を追加しろ」と指示→休日セルへ時間枠を足そうとして失敗。
- 🔴 さらに `set_cell_block` は休日セルでも「出勤」ラジオを押していた＝**休日→出勤変換でその日を"開けて"しまう**重大な事故リスク（売止の真逆・被りの一因と推定）。
- **休/出はセルのclassで判別できる**：休=`mod_btn_35_cur shiftdate`(txt"休")／出=`mod_btn_35_5 shiftdate`(txt"出")。

### 対策（実装・コミット対象は `C:\KowaBot`）
1. **自動再ログイン**：`store_sb_password.py`（社長が1回実行・ID/PWを**Windows資格情報マネージャー(keyring)に暗号化保管**＝平文/リポジトリに残さない＝原則3非抵触）／`sb_manager.auto_relogin()`（keyringのID/PWで実フォーム送信）／`main.py`のkeepalive(5分毎)・reconcile(毎時)に「切れ検知→auto_relogin→ダメな時だけ社長へ下書き通知」を配線。bot用ログインID＝**CE52480**。
2. **休日検知**：`_get_cell_entries` がclassで休日を判定し `'休'` を返す。
3. **diff_sbで休日スキップ**：休日＝全枠ブロック済み＝売止目的達成。出勤化（＝開放）はしない（社長の手動休日を尊重）。
4. **set_cell_block安全弁**：休日セルは一切触らない（`skip_kyujitsu`）。出勤化事故を封鎖。
5. **ログ精度**：`set_cell_block` が結果を返し、`main.py` は **実際に `ok` で書けた件数だけ**を「売止N件適用」と記録（旧「N件修正完了」は解除スキップも数えて紛らわしかった）。

### 実証（2026-06-18 実機ログ）
- **auto_relogin成功**：`16:02 SB session alive: False → submit=submitted → 16:02 session_alive=True`。以後切れても5分以内に自己再ログイン。
- **no_block0消滅**：dry-run 2回連続で `6/22 heijitsu(休)` へのno_block0指示が**確定的に消滅**。
- **書き込み実証**：本番reconcileで `SB set W001418166 day23: 16:00-18:00 (ok)` → commit → **直後の再読込で6/23が計画から消滅＝売止がSBに永続**。「書いても効かない」→「書けば確実に残る」を証明。

### 🔴 追加で判明した最重要バグ（2026-06-18 夕方）＝セルのインデックスずれ
- SBシフト表は**行の中央(15日と16日の間)に「平日 予約用」名前セルが1つ挟まる**。そのため旧コードの `children[day+2]` は**16日以降で1日ズレて隣の日**を読み書きしていた（実測: idx18=名前セル／realday16はidx19）。
- これが **late-month(16日以降)の「売止が効かない・隣の日に入る」の真因**。当日の本番reconcileも"6/23のつもりで6/22へ書く"実害を確認。
- **根治＝index依存を全廃し、セルを日付id `{staffId}_{YYYYMMDD}`（例 `W001418166_20260629`）で直接特定**（`sb_manager._get_cell_entries`/`get_month_shifts`/`set_cell_block` を修正）。
- 修正後の本番reconcileで **6/18〜30 全日がカレンダーの理由と一致**、6/22マツカワ様13-16の売止も正しく着地、7/19-20旅行売止も反映を**実機2回読みで検証**。検証ツール `C:\KowaBot\report_block_618_630.py`（引数 `YYYYMM START END` 可）。
- ⚠️ **社長への日付つき報告は必ずid方式の読み取りで出す**（index方式の旧値は信用しない）。

### 🔴 CHILLNN側も同種のインデックスずれ（2026-06-18 夜・修正済）
- `chillnn_manager` は在庫ボタンを通し番号 `プラン×日数+(日-1)` で当てていた。だが実体は**4行(tr)=4プラン**で、**予約が入った日は状態が「在庫無」**（売止/販売中/不可とは別の第4状態）になる。旧コードは在庫無を除外していたため、**予約が1件入るごとに通し番号がズレ、それ以降の全プラン/全日を誤って読み書き**していた（予約が増えるほど悪化）。
- **根治＝通し番号を廃止。プラン名の目印（`【平日】13`等）で行を特定 × セルindex(=日)** で直接ターゲット。在庫無も読み、`diff_chillnn` は在庫無/不可を触らない。
- 本番reconcileで6月分**21件を整列→読み直し「差分なし」を実機検証**。テーブル構造: `tr[0]`=日付ヘッダ（`1月`=1日, `children[d]`=d日）, `tr[1..4]`=4プラン。
- 📌 **教訓: SB(日付id)もCHILLNN(プラン行×日列)も「通し番号/index依存」は使わない。** 検証ツール `C:\KowaBot\diag_chillnn_*.py`（必要時再作成）。

### 残課題（フォロー・別優先度）
- ⚠️ `get_month_shifts` の読み取りがポップアップ開閉依存で**flaky**（実行ごとに揺れるのは無害な`→ -`解除行のみ／出勤セルの売止失敗には繋がらない）。将来、popupの開閉完了待ちを堅牢化するとノイズが減る。
- 自動再ログインの稼働は毎時reconcile/keepaliveのログ（`auto_relogin: session_alive=True`）で日次確認する。
- 診断ツール `C:\KowaBot\diag_sb_struct.py`（休/出・yoteiBlock0構造の実DOM採取・再利用可）を保全。
